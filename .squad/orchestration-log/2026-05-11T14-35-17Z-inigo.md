# Orchestration Log: inigo

**Date:** 2026-05-11T14:35:17Z  
**Agent:** inigo (Claude Sonnet 4.6)  
**Status:** IN PROGRESS  

## Tasks

1. **Fix /admin Route Auth Enforcement**
   - Implement `staticwebapp.config.json` route protection
   - Unauthenticated requests should redirect to `/.auth/login/aad`
   - Prevents exposure of dashboard HTML and API endpoint structure

2. **Fix Rate Limiting Persistence**
   - Current in-memory counter per Function instance is insufficient
   - Design solution using Azure Table Storage (recommended)
   - Implement persistent rate-limit counter with PartitionKey=IP, RowKey=timestamp-window
   - Verify 5 requests/minute/IP limit enforcement across all instances

## Blockers

- Both fixes required before P0 smoke tests can all pass
- Rate limiting persistence is architectural decision (Table Storage vs Redis vs in-memory with documented limitation)

## Success Criteria

- All 15 P0 smoke tests pass
- /admin returns 302 or 401 to unauthenticated requests
- Rate limit 429 responses consistently trigger after 5 requests per minute per IP
