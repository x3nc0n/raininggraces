# Orchestration Log — Westley (Lead)

**Session:** 2026-05-11T18:25:43Z  
**Mode:** background (one-shot)  
**Task:** Operations review of live photo app

## Summary

Operations review completed on live photo app deployment at `https://blue-hill-07daee510.7.azurestaticapps.net`. Identified two critical operational gaps blocking production release:

1. **Blob Storage CORS missing custom domain** — preflight requests from `https://photos.raininggraces.com` return 403; only SWA default hostname is allowed.
2. **Cleanup workflow not armed** — GitHub repository variables not configured in `x3nc0n/raininggraces-photos`; cleanup cron runs but has no `PUBLIC_APP_BASE_URL`, making the job no-op.

Both are core operational controls, not polish items. Photos must be deletable from the live domain, and the 30-day expiry promise requires the cleanup job to actually run.

## Outcome

Created GitHub issue `x3nc0n/raininggraces-photos#12` summarizing findings and recommendations. Decision captured in `decisions.md` (2026-05-11T13:18:46.291-05:00 entry). Team assigned:
- **Inigo / Fezzik:** Align upload/share routes with custom-domain path
- **Valerie:** Fix IaC and workflow configuration drift
- **Vizzini:** Re-run smoke tests on custom-domain paths after fixes

## Release Gate

Do not deploy to custom domain until both issues resolved and smoke tests pass.
