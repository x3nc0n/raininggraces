# Orchestration Log — Vizzini (Tester)

**Session:** 2026-05-11T18:25:43Z  
**Mode:** background (one-shot)  
**Task:** Staging test cases as GitHub issues

## Summary

Reviewed photo app codebase for QA issue creation. Created five tracking issues in `x3nc0n/raininggraces-photos` covering major test domains:

1. **Issue #1 (Auth & Security)** — `requireAdmin` guard, ADMIN_EMAIL fallback, identity principal extraction, role-based route access
2. **Issue #2 (Upload & Admin CRUD)** — Album creation, SAS token generation, photo upload via preflight, confirmation flow, entity normalization, password reset
3. **Issue #3 (Share & Download)** — Share token verification, password verification, gallery reveal logic, ZIP download generation with password in query string
4. **Issue #4 (Retention & Cleanup)** — 30-day expiry calculation, cleanup job execution, blob lifecycle policy backup, orphaned album cleanup
5. **Issue #5 (Edge Cases & Error Handling)** — Empty albums, large files, special characters in names, concurrent uploads, missing Table Storage entities, rate limiting on password verification

All issues labeled `test` + priority (`P0`–`P2`). Test cases reference code anchors (`api/helpers/*.js`, `staticwebapp.config.json`, `src/assets/js/*.js`, `.github/workflows/cleanup-expired.yml`).

## Outcome

Staged test suite is ready for execution. Smoke tests identified two critical failures (captured in `decisions.md`):
- `/admin` route unprotected (missing `allowedRoles` in SWA config) — CRITICAL
- Rate limiter not working due to multi-instance Consumption plan behavior — HIGH

Team tasked with remediating these before P0 test suite can pass.
